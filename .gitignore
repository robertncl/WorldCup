# Frontend build output & dependencies
/frontend/dist/
/frontend/node_modules/
# bun.lock is committed so the dependency SHA-512 integrity pins are enforced
# on every checkout / CI install.
/frontend/*.tsbuildinfo

# Go build artifacts
/backend/worldcup
/backend/worldcup.exe
*.test

# Editor / OS
.DS_Store
.idea/
.vscode/
*~
